'use strict';

module.exports = {
  type: 'string',
  description: 'page name that will be used to create canonical link. e.g. value "/abc.html" would create canonical link "${YOUR_HOST}/abc.html". Generally we would use the current page as canonical link. If value not given, then no canonical link will be added to the page',
};
